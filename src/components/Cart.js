import React from 'react';

//styles
import styles from "./Cart.module.css";

const Cart = () => {
    return (
        <div className={styles.Cart}>
            
        </div>
    );
};

export default Cart;